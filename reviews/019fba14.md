# Review: `arena/019fba14-celup-lab`

## Scope and delta

Tip `8cc3726` is a large v7.x resampling line: it changes `celup_lab.c`, build/docs, staircase testing, and the sheet generator; adds 30+ WebP comparison/fixture artifacts; and deletes the two autodeblur notes. Snapshot delta is 40 files, +670/−688. Its history describes new/changed dehourglass, smooth, consistentcompress, scale2x, and a horizontal-only `linear` mode.

## Comparison

- This is a different approach from autodeblur branches `019fba18`, `019fba1b`, `019fbef6`, and `019fbf78`: it changes the general resampling/mode behavior rather than incrementally repairing the v4.9 autodeblur fit.
- It targets staircase/hourglass metrics like `019fba18` and `019fbf78`, but does so with broad mode rewrites and bundled generated sheets instead of a narrowly isolated deblur method.
- It has no ancestry relationship establishing it as a successor to the v4.9.9/analytical branches.

## Review finding

**Hold/reject for master until isolated A/B testing.** The branch is too broad for a confidence merge: algorithm changes, test threshold changes, docs, build changes, and generated binaries are interleaved. Commit-message metrics are promising but not independently verified here, and the `linear` mode's intentionally nearest-neighbor vertical direction needs visual/API review.

## Integration option

Split into: (1) a reviewed `linear` mode with CLI/help/tests, and (2) any dehourglass/scale2x changes with baseline metric comparisons. Keep generated sheets out of the functional change unless release documentation specifically needs them.
