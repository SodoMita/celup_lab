# Review: `arena/019fba18-celup-lab`

## Scope and delta

Tip `18df5b2` is the v4.9.9 autodeblur completion line. It changes the core, README/handoff, staircase check, and adds a metrics helper and two real-image sheets (10 files, +2,219/−547 against the snapshot). The stated principal correction replaces axis-aligned pass-1.5 evidence blur with tangent-marched bilinear/tent integration, intended to prevent normal-direction flank mixing at contour poles. It also adds quantized-source wash admissions, endpoint extension, core rescue, and straggler handling.

## Comparison

- `019fbf78` shares history through v4.9.9 WIP commit `5d5f7a8`, but **does not contain this tip**; its analytical work is a competing continuation, not a superset of this final tangent-oriented correction.
- `019fbfb9` starts from master and offers an early analytical method; it is not a replacement for this mature v4.9.9 line.
- `019fbef6` compares itself against this line but diverges from the master snapshot and focuses on texture gain/cap tuning rather than this evidence-transport repair.
- `019fba1b` and `019fbcda` pursue alternative consensus/SDF/corner changes with overlapping visual goals but no demonstrated common final state.

## Review finding

**Strongest candidate among the autodeblur-fit continuations, but do not bulk merge.** It has the clearest diagnosis, targeted core mechanism, stated gates (`check_stairs`, `check_corners`, `test_scales`), and docs. Risk remains high because the snapshot delta includes replacement/deletion of historical notes and unverified metric claims.

## Required integration checks

Port the final core/test/doc commits into a clean master baseline; build with the documented WebP setup; run all three named gates plus real-image visual diffs at the documented `-r 6` and `-r 2.3` recipes. Reject if pole improvement trades off corners, alpha, or intentional blur.
