# Review: `arena/019fba1b-celup-lab`

## Scope and delta

Tip `1067d91` proposes “C1-continuous 3×3 consensus DSDF across arbitrary upscales.” It modifies the core, scale tests, evaluation scripts, docs, and comparison generation; adds many example/test images and sheets; and deletes the autodeblur notes (28 files, +884/−562).

## Comparison

- It overlaps `019fbcda` in the corner/SDF/halo problem space, but `019fbcda` explicitly backs away from unstable mass-conserving depth toward ERF fitting plus a local envelope clamp. These are competing remedies, not changes that should be stacked blindly.
- It overlaps `019fba14` on arbitrary-scale/staircase quality, but uses a DSDF consensus approach rather than that branch's broader v7 resampler modes.
- Compared with `019fba18`, it is not the tangent-oriented v4.9.9 autodeblur completion and has less explicit final-gate evidence in its tip message.

## Review finding

**Hold for controlled A/B evaluation.** C1 continuity and a 3×3 consensus are plausible, but this is a core rendering rewrite combined with large asset churn and evaluation-script changes. The branch's own test/evaluator modifications cannot alone prove a regression improvement.

## Required integration checks

Extract the DSDF core patch only; retain master fixtures. Run existing scale/stair/corner tests unchanged, then compare hourglass, halo, edge-position, alpha, and intentional-soft-gradient behavior against `019fba18` and `019fbcda`. Merge only if it wins on pre-existing gates without weakening them.
